# Codex Cross-Review — Hara v1.6 cleanup round (codex meta-review B1~M12 closure)

## Context

이전 codex meta-review에서 2 blockers + 8 majors + 2 minors + 1 info 발견. 본 v1.6 round는 **cleanup only — no new features**. 사용자 원칙 *"obsolete 적극 제거"* 회복 + adaptive learning loop 회복 + size discipline 회복.

## Changes applied

### B1 — Stranger-proof 회복
- `examples/starpin/.harness/status.md`: v0.1.0 시기 (609줄) → v0.4 기준 *current-only* 재작성. root STATUS와 cross-reference

### B2 + size discipline
- HARNESS.md Fleet "AST-level mechanical enforcement" → "AST-level enforcement" + honest 제한 명시 (sibling-file + parent reach-around 미커버 사실 인정; F122/F123 carry-over 명시)
- HARNESS.md §9 + §10 (Phase E criteria — v1.0 ship 후 historical) → `docs/history/phase-e-dogfood-criteria.md`로 archive

### M3 — 적응형 learning loop 회복 (핵심)
- `templates/SUBTREE-PROMPT.template.md`: Required reads에 *capabilities.md + 명시된 모든 local skill 파일 직접 읽기* 추가
- `Local skill application` 섹션 신설 (applied / not_applicable / gap_detected — 모든 active skill 별 의무)
- *child가 local skill을 우회하지 못하도록* enforce

### M6 — starpin RELEASE 정직화
- `examples/starpin/RELEASE.md`: "production-ready hardening" → "backend hardening only"
- Completion definition 4-layer 분할 (backend-ready ✓ / app-alpha ✗ / data-ingest-ready ✗ / deploy-ready ✗)

### M8 — REVIEW + MERGE-REPORT capability_candidate 의무화
- `templates/REVIEW.template.md`: Finding 항목에 `capability_candidate: yes/no + 사유` 필드 추가. missing 시 closure 무효
- `templates/MERGE-REPORT.template.md`: `Local skills applied` 섹션 + `capability_candidate` 의무화 (REVIEW와 sync)

### M10 — child failure recovery (Fleet F10 신설)
- HARNESS §14 표에 **F10** 추가: child rate-limit/crash 시 parent 대리 작성 절차 (`parent_authored: true` + `evidence_confidence` 명시)
- starpin v0.4 F1 rate-limited evidence 명시

### M4 + M7 — validators / ledger
- `scripts/fleet/validate_capabilities.py` 신설 — capabilities.md schema validator (front-matter + yaml-block entry count + file existence + canonical status)
- `scripts/_codex_postprocess.py`: review postprocess 시 `.harness/codex-token-ledger.jsonl` 자동 append (JSONL machine-parseable — STATUS 수동 추정 끝)

### M9 — ESLint AST real fix (F122 + F123 closure)
- `scripts/fleet/gen_eslint_lock.py`: `public_module_path` 파서 + Layer 4 (same-dir sibling file deny) 추가
- `templates/LOCKED-INTERFACE.template.md` front-matter: `public_module_path` + `consumed_stable_modules` machine-readable field 신설
- v1.3 "AST-level mechanical" claim과 *현실 일치* (sibling-file boundary catch 가능)

### m11 — Local skills split proof (2 of 6)
- `examples/starpin/.harness/skills/sky-coords-fixture/` 디렉토리 신설: `rationale.md` (full original) + `checklist.md` (≤80줄 short form for child prompt injection)
- `examples/starpin/.harness/skills/claim-exclusivity-contract/` 동일 split
- 나머지 4 skills (auth-rotation / mobile-bundle / external-catalog / geolocation-pii)는 *v1.7 carry-over* (pattern 검증 후 점진 적용)

### m12 — Dead artifact archive
- `project-types/_generic/esm-jest-pattern.md` → `project-types/_incubating/`
- `templates/LOCAL-ROLE.template.md` → `project-types/_incubating/`
- `_incubating/README.md` 작성 — promotion 경로 명시

### ADR-001~007 cleanup (M5)
- DECISIONS.md: ADR-001~007 본문 → summary table (Phase A 시기 결정, current base 흡수됨)
- full text는 `docs/history/adrs-001-to-007.md`에 보존

## Review focus

1. **B1 closure**: starpin local STATUS가 정말 current-only? root STATUS와 일치?
2. **M3 closure**: SUBTREE-PROMPT의 Local skill application section이 *child가 우회 어렵게* 설계됐나? (gap_detected → capability candidate 자동 등록 의무)
3. **M9 ESLint enhancement**: `public_module_path` Layer 4 (same-dir sibling deny)가 starpin v0.3 OAuth case (apple.ts → google.js)를 *진짜* catch하는지 검증 (manual eslint test 추천)
4. **m11 split pattern**: rationale + checklist 분리가 *진짜 가치*인가? child prompt가 checklist만 받아도 compliance 가능?
5. **m12 archive 영향**: incubating으로 옮긴 esm-jest-pattern + LOCAL-ROLE의 *외부 reference* (현 base에서 가리키는 곳) 없는지?
6. **새 attack vectors**: cleanup으로 *break된* 것 없나? (e.g. `templates/LOCAL-ROLE.template.md` 가리키는 곳, ADR-001~007 본문 reference 등)
7. **HARNESS body size**: v1.5 → v1.6 cleanup으로 줄였나? (지금 약 580줄 — 이전 v1.3 600줄 가벼움; honest 한계 추가로 살짝 늘어남)
8. **Hidden gaps**: cleanup이 *놓친* dead artifact 또는 *추가로 obsolete된* 항목

## Out of scope
- v1.5 → v1.6 cleanup이 본 round의 핵심. *기능 추가 없음*. 다음 round (v1.7 또는 starpin v0.5)는 별도.

## Review format
REVIEW 양식 — severity (blocker/major/minor/nit/info) + 위치 + 근거 + 제안. **각 finding의 capability_candidate field 의무화 (v1.6 M8 신설)**.
