---
artifact: review
date: 2026-05-29
author: codex
status: open
severity: minor
target: starpin v0.25.2 ISS — r3 confirmation (basis aligned)
review_round: r3
prior_review: .harness/reviews/20260529-starpin-v025-iss-r2.md
codex_meta:
  codex_version: 0.132.0
  model: gpt-5.5
  provider: openai
  session_id: 019e71ed-806f-74e1-a66a-1238e68c205e
  workdir: /Users/satgym/work/harness
  sandbox_policy: workspace-write [workdir, /tmp, $TMPDIR]
  reasoning_effort: high
  tokens_used: 120378
  base_ref: 
  included_paths: 
  invoked_at: 2026-05-29T04:10
  prompt_source: examples/starpin/.harness/prompts/v025-iss-tracking-review-r3.md
---

**REVIEW — verdict: minor**

Findings:

- `severity: minor`  
  `위치`: [service.test.ts](/Users/satgym/work/harness/examples/starpin/backend/tests/unit/iss/service.test.ts:160)  
  `근거`: 새 regression test는 r2 basis fix를 실제로 lock하지 못합니다. 같은 fixture에서 r1 방식의 spherical observer ECF를 재현해 계산하면 `decFromLatSph = 21.713`, `zenithDistance = 38.041`라서 현재 assertion `≤ zenithDistance + 1`을 여유 있게 통과합니다. 즉 reverted spherical basis여도 실패하지 않습니다.  
  `제안`: 이 테스트는 고정 fixture의 WGS84 topocentric RA/Dec reference value를 직접 assert하도록 바꾸는 게 낫습니다. 예: `2026-05-29T10:40:00Z`, Seoul 기준 현재 값은 대략 `ra=211.532029`, `dec=18.258618`; spherical revert는 `ra=211.089717`, `dec=15.853497`라서 `toBeCloseTo(..., 2)` 수준으로도 실패합니다.

Confirmations:

1. `service.ts`는 alt/az와 topocentric RA/Dec 모두 같은 WGS84 observer basis를 씁니다. `ecfToLookAngles(observerGd, satEcf)`와 `geodeticToEcf(observerGd)`가 같은 `observerGd`에서 파생됩니다.
2. `tle.ts` header는 현재 TLE를 Celestrak real GP-data로 일관되게 설명합니다. stale “current synthetic TLE” 문제는 닫혔습니다.
3. 새 basis-consistency test는 통과하지만, r1 spherical basis revert를 잡지는 못합니다.
4. Validation confirmed locally: `npm --prefix backend run build` clean, `npm --prefix backend test` = 46 suites pass, 472 pass / 3 skip.
