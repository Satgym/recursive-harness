---
artifact: review
date: 2026-05-28
author: codex
status: open
severity: major
target: Hara v2.3.1 r2 verify (3 r1 codex blockers/major)
review_round: r2
prior_review: .harness/reviews/harness-20260528-v231-r1.md
codex_pass: false
blocker_count: 0
major_count: 1
minor_count: 1
codex_meta:
  codex_version: 0.132.0
  model: gpt-5.5
  provider: openai
  session_id: 019e6ebe-9737-7c22-8766-1e6cd6879f2c
  workdir: /Users/satgym/work/harness
  sandbox_policy: workspace-write [workdir, /tmp, $TMPDIR]
  reasoning_effort: high
  tokens_used: 101157
  base_ref: 
  included_paths: 
  invoked_at: 2026-05-28T13:20
  prompt_source: .harness/prompts/harness-v231-r2.md
---

**Findings**

1. `severity: major`  
   `위치`: [scripts/_codex_postprocess.py](/Users/satgym/work/harness/scripts/_codex_postprocess.py:98), [scripts/ui-visual-review.sh](/Users/satgym/work/harness/scripts/ui-visual-review.sh:92), [scripts/ui-visual-review.sh](/Users/satgym/work/harness/scripts/ui-visual-review.sh:162)  
   `근거`: canonical FM parser가 inline YAML comments를 실제로 허용하지 않습니다. regex가 `#` 전까지만 capture한다고 되어 있지만, `\s*$` 때문에 `codex_pass: true # ok` 라인은 match 자체가 실패합니다. default prompt 예시는 `codex_pass: true        # or: false`라서, Codex가 예시를 그대로 따르면 postprocess merge도 실패하고 `ui-visual-review.sh` FM parse도 실패합니다. Probe 결과 `comments {}` / `fm_comment codex_pass='' blocker=''`.  
   `제안`: FM regex를 comment-aware로 바꾸세요. 예: value capture 뒤에 optional comment를 허용하는 `^\s*key\s*:\s*([^#\n]*?)\s*(?:#.*)?$` 계열로 통일하고, bool/int 검증은 strip 이후 별도 적용.

2. `severity: minor`  
   `위치`: [scripts/_codex_postprocess.py](/Users/satgym/work/harness/scripts/_codex_postprocess.py:153)  
   `근거`: `body_region`은 `\ncodex\n`와 `\ntokens used\n`가 둘 다 있을 때만 설정됩니다. 둘 중 하나라도 CLI 출력 변경으로 빠지면 canonical verdict merge가 조용히 비활성화됩니다. Probe에서 `missing_tokens`와 `missing_codex` 모두 verdict keys가 wrapper FM에 나타나지 않았습니다.  
   `제안`: marker path 실패 시 `extract_canonical_verdict(raw)` 또는 `extract_review_body` 이전 body 후보로 fallback하되, Codex CLI metadata separator와 충돌하지 않게 “first actual leading FM after codex marker if present, otherwise first FM block containing canonical keys”로 제한하세요.

**Section A**

Clean inner FM with all 4 keys는 모두 merge됩니다. Partial FM도 해당 key만 merge됩니다. No inner FM은 `{}`로 wrapper unchanged입니다. Trailing punctuation은 `rstrip(".,;")`로 처리됩니다. Inline YAML comments는 실패합니다.

`body_region` slicing은 정상 Codex CLI shape에서는 동작하지만 marker/footer가 없으면 silent failure입니다. merge order는 `prior_review` 다음, `codex_meta:` 앞이라 first-block FM parser가 읽기에는 안정적입니다.

**Section B**

Body fallback end-anchoring은 r1 blocker #2를 닫습니다. line-only `key: value`만 받고, trailing whitespace/tab/CR 및 final line without newline도 통과합니다. `codex_pass: true. HC-13 ...` 같은 trailing prose는 거부됩니다.

**Section C**

Round-aware canonical path는 적용되어 있습니다: [scripts/ui-visual-review.sh](/Users/satgym/work/harness/scripts/ui-visual-review.sh:137). Evidence patch도 같은 `CODEX_REVIEW_OUT` 값을 `codex_review`에 기록합니다: [scripts/ui-visual-review.sh](/Users/satgym/work/harness/scripts/ui-visual-review.sh:222). r1/r2는 `ui-codex-<date>-<slug>-r1.md`와 `...-r2.md`로 분리됩니다. Default `r1`은 functional break는 아니지만, skill docs still mention no-suffix names.

**Regression Sweep**

Ran focused probes against existing `examples/starpin/.harness/reviews/ui-codex-20260528-telescope-features-smoke.md`: stripped body stayed identical and clean verdict keys merged into wrapper FM. Existing manually patched visual review files parse through FM path. I did not execute full `ui-visual-review.sh` because it invokes Codex CLI; path construction and evidence patch argv were inspected directly.

**Coverage**

- Finding 1: `partial` — clean FM fixed, commented YAML and marker-loss fallback still brittle.
- Finding 2: `closed`.
- Finding 3: `closed` functionally; minor doc drift remains in skill/output comments.
