---
artifact: review
date: 2026-05-28
author: codex
status: open
severity: major
target: Hara v2.4 — check-subagent-prompt.sh enforceable lint
review_round: r1
codex_pass: true
blocker_count: 0
major_count: 0
minor_count: 3
codex_meta:
  codex_version: 0.132.0
  model: gpt-5.5
  provider: openai
  session_id: 019e6f0b-914c-7a50-8869-c498bd938953
  workdir: /Users/satgym/work/harness
  sandbox_policy: workspace-write [workdir, /tmp, $TMPDIR]
  reasoning_effort: high
  tokens_used: 73277
  base_ref: 
  included_paths: 
  invoked_at: 2026-05-28T14:44
  prompt_source: .harness/prompts/harness-v24-r1.md
---

**REVIEW**

Assumption: `codex_pass: true` means no blocker/major. The three items below are patch-or-carry minors.

1. `severity: minor`  
   `위치`: [scripts/check-subagent-prompt.sh](/Users/satgym/work/harness/scripts/check-subagent-prompt.sh:68)  
   `근거`: `--strict` regex가 `\.harness/reviews/.*impl.*\.md`라서 `.harness/reviews/04-20260528-implementation-notes.md`도 PASS합니다. 메시지는 `...-impl.md`를 요구한다고 말하므로 strict contract보다 구현이 느슨합니다.  
   `제안`: `\.harness/reviews/[^[:space:]`)]*-impl\.md([[:space:]`),]|$)`처럼 `-impl.md` suffix를 직접 요구.

2. `severity: minor`  
   `위치`: [PATTERNS.md](/Users/satgym/work/harness/PATTERNS.md:281)  
   `근거`: wrapper API가 파일 경로만 받기 때문에 starpin v0.18처럼 `Agent()` inline string으로 dispatch되는 prompt는 lint 대상이 아닙니다. 현재 문구는 `<prompt.md>`를 예시로 보여주지만, inline prompt는 반드시 `.harness/prompts/*.md`로 먼저 persist해야 한다는 규칙이 명시되지 않았습니다.  
   `제안`: “inline Agent prompt도 dispatch 전 `.harness/prompts/<slug>.md`로 저장한 뒤 이 스크립트를 실행한다. stdin/inline lint는 v2.4.1+ hook carry.” 한 문장 추가.

3. `severity: minor`  
   `위치`: [STATUS.md](/Users/satgym/work/harness/STATUS.md:20)  
   `근거`: Current/Recent ships는 v2.4 pending으로 갱신됐지만 Active gate는 여전히 “Hara v2.3.2 ship 준비”, “starpin v0.18 wholesale 준비”, “Autonomous multi-ship 진행 중”처럼 과거 진행 상태를 active로 표현합니다. HARNESS §8의 stranger-proof status 기준에서 다음 세션이 현재 gate를 오해할 수 있습니다.  
   `제안`: Active gate를 “Hara v2.4 r1 review 진행 중, open findings N” 중심으로 압축하고 완료된 v2.3.2/v0.18 내용은 Recent ships에만 남기기.

검증 결과: heading regex는 `### 1. Code`, `## Code`, `### Code (NEW + MODIFY)`, `Fixture / seed`, `impl review`, `impl-review`, `Test`/`Tests` 모두 통과했습니다. `ImplementationReview`는 false positive가 아니었습니다. v0.16 legacy prompt는 5/5 missing으로 FAIL했고, 좋은 prompt와 PATTERNS 예시 형태는 PASS했습니다. `bash -n scripts/check-subagent-prompt.sh`도 통과했습니다.
