---
artifact: review
date: 2026-05-28
author: codex
status: open
severity: major
target: Hara v2.4 r2 — verify 3 minor closures
review_round: r2
prior_review: .harness/reviews/harness-20260528-v24-r1.md
codex_pass: true
blocker_count: 0
major_count: 0
minor_count: 2
codex_meta:
  codex_version: 0.132.0
  model: gpt-5.5
  provider: openai
  session_id: 019e6f0f-0764-7d72-a757-ab1153faa7cd
  workdir: /Users/satgym/work/harness
  sandbox_policy: workspace-write [workdir, /tmp, $TMPDIR]
  reasoning_effort: high
  tokens_used: 131204
  base_ref: 
  included_paths: 
  invoked_at: 2026-05-28T14:48
  prompt_source: .harness/prompts/harness-v24-r2.md
---

r2 결과: blocker/major는 없습니다. 다만 r1 minor closure가 완전히 clean 하지는 않습니다.

minor: `scripts/check-subagent-prompt.sh:70` strict regex는 `*-implementation-notes.md`와 `*-impl-notes.md`를 제대로 FAIL 처리하고, `*-impl.md`는 PASS합니다. `[^[:space:]`)]*`도 `grep -E`에서 깨지지 않습니다. 다만 `.harness/reviews/foo-impl.md.`처럼 문장 끝 마침표가 붙은 정상 prose path를 FAIL 처리합니다. comma / `)` / backtick은 PASS했습니다. 제안: sentence punctuation까지 의도하면 trailing delimiter에 `.`를 포함하거나, prompt 규약을 “path는 backtick으로 감싼다”로 명시하세요.

minor: `STATUS.md:17-29` Active gate compression이 아직 stranger-proof하지 않습니다. `r1 review 진행 중`이라고 되어 있는데 현재는 r2 verify 단계이고, `Autonomous multi-ship 진행 중: v0.16 ... v0.18 ...` 및 `carry to v0.16` 같은 과거/불가능 상태가 Active gate에 남아 있습니다. r1 Minor #3은 v2.3.2/v0.18 준비 라인은 제거됐지만 “현재 진행만 표시” 기준은 아직 미충족입니다.

PATTERNS inline `Agent()` 보강은 명확합니다. inline prompt도 dispatch 전 `.harness/prompts/<slug>.md`로 persist 후 wrapper 실행해야 한다는 의무와, v2.4.1+ pre-Agent hook carry 경로가 둘 다 읽힙니다.

Regression 확인: `bash -n scripts/check-subagent-prompt.sh scripts/codex-bundle-review.sh scripts/ui-visual-review.sh` 통과. sister script 쪽 호환성 문제는 보이지 않습니다. r1에서 확인한 legacy prompt 실패 조건도 유지됩니다. 별도 persisted v0.16 prompt 파일은 `.harness/prompts/`에서 찾지 못해 동일 파일 재실행은 못 했지만, 5 heading 없는 old-style prompt class는 계속 exit 1입니다.
