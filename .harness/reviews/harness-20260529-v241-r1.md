---
artifact: review
date: 2026-05-28
author: codex
status: open
severity: major
target: Hara v2.4.1 — --mode=auto flag
review_round: r1
codex_pass: false
blocker_count: 0
major_count: 1
minor_count: 1
codex_meta:
  codex_version: 0.132.0
  model: gpt-5.5
  provider: openai
  session_id: 019e6f2d-c1b1-7df2-af0c-1c27ea023af4
  workdir: /Users/satgym/work/harness
  sandbox_policy: workspace-write [workdir, /tmp, $TMPDIR]
  reasoning_effort: high
  tokens_used: 80567
  base_ref: 
  included_paths: 
  invoked_at: 2026-05-28T15:22
  prompt_source: .harness/prompts/harness-v241-r1.md
---

major: `위치`: [scripts/check-subagent-prompt.sh](/Users/satgym/work/harness/scripts/check-subagent-prompt.sh:41). `근거`: `--mode` 값이 빠진 호출이 정상적인 invalid-mode exit로 끝나지 않고 루프에 빠집니다. `shift 2`가 인자 부족으로 실패해도 `set -e`가 없어서 계속 진행되고, `$#`가 줄지 않아 반복됩니다. `perl -e 'alarm 2; exec @ARGV' bash scripts/check-subagent-prompt.sh --mode`로 2초 후 timeout(exit 142) 재현했습니다. `제안`: `--mode)` 분기에서 `$# -ge 2`와 `${2:0:1} != "-"`를 확인하고, 없으면 exit 3으로 즉시 실패시키세요.

minor: `위치`: [scripts/check-subagent-prompt.sh](/Users/satgym/work/harness/scripts/check-subagent-prompt.sh:23). `근거`: help/docstring은 `*-impl*.md`라고 설명하지만 실제 regex와 PATTERNS는 suffix convention인 `*-impl.md` / `*-impl-r<N>.md`입니다. 이 차이 때문에 `*-impl-vN.md`가 auto impl로 보일 수 있지만 실제로는 review skip 됩니다. `제안`: docstring과 line 57 comment를 suffix 기준으로 정확히 바꾸세요.

Section A: `-impl` suffix convention은 충분합니다. `*impl*` substring으로 넓히면 `implementation`, `impl-notes`, `impl-vN` 같은 drift를 조용히 허용할 가능성이 커집니다. nested dir은 `basename "$PROMPT"`를 쓰므로 의도대로 동작합니다.

Section B: `--mode=value`, `--mode value`, `--strict` 순서 독립성은 정상입니다. unknown flag exit 3도 괜찮습니다. 단, bare `--mode` hang은 ship 전에 고쳐야 합니다.

Section C/D: 기본 auto로 review prompts를 graceful skip하는 변경은 dogfood 문제 해결로 수용 가능합니다. PATTERNS 보강은 방향이 맞고, HC-11상 v2.4.1도 ship-style이면 r1+r2 review file이 필요합니다.
