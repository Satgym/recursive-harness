---
artifact: review
date: 2026-05-28
author: codex
status: open
severity: major
target: Hara v2.5 — PATTERNS dom-mutation-order + smoke-setup + SMOKE_FRESH_SIM env
review_round: r1
codex_pass: false
blocker_count: 0
major_count: 1
minor_count: 2
codex_meta:
  codex_version: 0.132.0
  model: gpt-5.5
  provider: openai
  session_id: 019e70dd-6180-7460-a0ef-cb3b5a352650
  workdir: /Users/satgym/work/harness
  sandbox_policy: workspace-write [workdir, /tmp, $TMPDIR]
  reasoning_effort: high
  tokens_used: 97279
  base_ref: 
  included_paths: 
  invoked_at: 2026-05-28T23:13
  prompt_source: .harness/prompts/harness-v25-r1.md
---

severity: major  
위치: `examples/starpin/scripts/run-mobile-smoke.sh:29-40`, `:107`  
근거: `SMOKE_FRESH_SIM=1`이 booted simulator 중 첫 번째 UDID만 shutdown합니다. 이후 `maestro test`는 `DEVICE_ID`를 넘기지 않으므로, 여러 iOS simulator가 Booted 상태이면 stale system dialog가 남은 다른 simulator를 Maestro가 잡을 수 있습니다. env var의 목적이 “fresh sim”이면 다중 booted 상태를 닫아야 합니다.  
제안: v2.5 설명대로 `xcrun simctl shutdown booted 2>/dev/null || true`를 쓰거나 모든 `state == Booted` UDID를 enumerate해서 전부 shutdown한 뒤 하나만 boot하세요.

severity: minor  
위치: `PATTERNS.md:384`  
근거: Maestro 공식 문서상 iOS `clearState`는 앱 reinstall로 app data folder를 제거합니다. “Capacitor localStorage만 clear”는 부정확합니다. 실제 lesson은 “system-level dialog는 app state reset으로 안 닫힌다”입니다.  
제안: “`clearState` resets app data/reinstalls app on iOS, but does not guarantee dismissal of simulator/system overlays”로 수정하세요. 참고: https://docs.maestro.dev/reference/commands-available/clearstate

severity: minor  
위치: `PATTERNS.md:388-399`  
근거: 문서가 `scripts/run-mobile-smoke.sh`를 가리키지만 현재 repo에서 실제 파일은 `examples/starpin/scripts/run-mobile-smoke.sh`뿐입니다. 또한 “옵션 추가 (v2.6 carry)”라고 되어 있어 v2.5 mitigation인지 v2.6 carry인지 헷갈립니다.  
제안: `<project>/scripts/run-mobile-smoke.sh` 또는 `examples/starpin/scripts/run-mobile-smoke.sh`로 명확히 쓰고, v2.6 carry는 grep enforcement 항목에만 남기세요.

Section A: §dom-mutation-order는 anti-pattern/correct pattern 차이가 명확하고 subagent prompt imperative도 따르기 쉽습니다. `newsletter.ts:204-213`의 실제 v0.20 fix도 `fetchNews` 후 `while` cleanup을 끝낸 뒤 `mountTodayWidget`을 호출하므로 패턴과 일치합니다. v2.6 grep enforcement 방향도 적절하지만, 단순 키워드 grep은 false positive가 쉬워서 “touch existing file with removeChild cleanup” 조건을 유지하는 편이 낫습니다.

Section B/C/D: env var opt-in은 backward compat 측면에서 합리적이고 default 1로 바꿀 필요는 아직 없습니다. Maestro `runFlow.when.visible.commands` syntax는 공식 docs의 조건부 inline subflow 형태와 맞습니다. bash+python 조합도 기존 스크립트 스타일과 일관되며 `state == 'Booted'` 판정 자체는 맞지만, 현재 구현은 첫 booted만 처리하는 점 때문에 r1 pass로 보긴 어렵습니다.
