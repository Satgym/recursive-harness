---
artifact: review
date: 2026-05-28
author: codex
status: open
severity: major
target: Hara v2.5 r2 verify (multi-sim + PATTERNS)
review_round: r2
prior_review: .harness/reviews/harness-20260529-v25-r1.md
codex_pass: false
blocker_count: 0
major_count: 0
minor_count: 2
codex_meta:
  codex_version: 0.132.0
  model: gpt-5.5
  provider: openai
  session_id: 019e70e1-ca39-7d41-af49-d6a4dac7530f
  workdir: /Users/satgym/work/harness
  sandbox_policy: workspace-write [workdir, /tmp, $TMPDIR]
  reasoning_effort: high
  tokens_used: 80284
  base_ref: 
  included_paths: 
  invoked_at: 2026-05-28T23:18
  prompt_source: .harness/prompts/harness-v25-r2.md
---

Major closure는 확인했습니다. `examples/starpin/scripts/run-mobile-smoke.sh:27`에서 `SMOKE_FRESH_SIM=1`과 `PLATFORM=ios` 조건이 같이 걸려 있어 Android emulator에는 영향이 없고, `xcrun simctl shutdown all` 뒤 `sleep 2` 후 기존 boot detection으로 이어집니다. 기본값 `SMOKE_FRESH_SIM=0`에서는 해당 블록이 실행되지 않으므로 backward compatibility도 유지됩니다. `bash -n`도 통과했습니다.

Minor #1 closure도 확인했습니다. `PATTERNS.md:384`의 `clearState` 설명은 Maestro 공식 문서의 iOS 동작, 즉 앱 재설치와 데이터 폴더 제거 설명과 정합합니다: https://docs.maestro.dev/reference/commands-available/clearstate. simulator-level overlay dismiss 보장 없음이라는 보강도 표현상 무리 없습니다.

Finding 1: severity minor, 위치 `PATTERNS.md:400`. 근거: r1 패치 설명은 v2.5 mitigation section의 `v2.6 carry` 라인을 제거했다고 했지만, 해당 문장이 아직 남아 있습니다. 제안: 이 줄은 제거하고 `PATTERNS.md:371`의 DOM mutation section에만 carry를 남기세요.

Finding 2: severity minor, 위치 `PATTERNS.md:377`. 근거: smoke setup 도입 문단의 workaround가 여전히 `xcrun simctl shutdown booted && xcrun simctl boot`로 남아 있어, r1 major의 multi-sim leak 교훈과 `shutdown all` mitigation을 흐립니다. 제안: 해당 문장을 `xcrun simctl shutdown all` 기준으로 갱신하세요.

결론: code-side major는 닫혔지만 PATTERNS clarity closure가 아직 불완전합니다.
