# ADR-001 ~ ADR-007 (Phase A 골격 결정) — archived 2026-05-27 v1.6 cleanup

> **Status**: archived. 본 결정들은 *Phase A 시기 골격 정착*으로 현 base (HARNESS v1.3+)에 모두 흡수됨.
> DECISIONS.md에는 summary table만 유지; 본 파일은 *full context*가 필요할 때만 참조.

## ADR-001 — 하니스는 git repo로 배포, 메타-부트스트랩

**Date**: 2026-05-25 · **Status**: accepted

**Context**: 하니스 배포 방식 후보: (a) 단일 SoT + 심볼릭링크 (b) 프로젝트마다 복사 (c) git repo로 만들어 clone/submodule.

**Decision**: (c) git repo. 프로젝트는 하니스 repo를 reference. 프로젝트별 적응적 규칙/스킬은 하니스 자체를 사용해 만든다 (메타 부트스트랩).

**Consequences**:
- 하니스 자체에 버전 필요 (`VERSION` 파일).
- 프로젝트는 `.harness/VERSION-PIN`으로 사용한 버전 기록.
- 하니스 업그레이드 시 마이그레이션 가이드 필요.

---

## ADR-002 — Codex 개입은 파일 기반 비동기를 기본으로

**Date**: 2026-05-25 · **Status**: accepted

**Context**: VSCode 환경 + Claude가 주 대화 상대. Codex 개입 방식 후보: (A) 파일 기반 비동기 호출 (B) MCP 즉석 호출 (C) 사용자가 별도 codex 세션 운영.

**Decision**: A를 기본 채널로. B는 후순위. C는 `AGENTS.md` + `INBOX/` 컨벤션으로 자연스럽게 지원.

**Consequences**: 모든 리뷰가 파일로 남아 재현/감사 가능. MCP는 v1.0 이후 옵션.

---

## ADR-003 — Codex 모델/계정은 사용자 설정

**Date**: 2026-05-25 · **Status**: accepted

**Decision**: 모델명은 코드/스크립트에 하드코딩 금지. `.harness/config.toml`의 `[models]` 섹션에서 `review`, `exec` 모델 각각 지정.

```toml
[models]
review = "gpt-5.5"
exec   = "codex5.3"
```

**Consequences**: 정확한 모델 식별자는 사용자 채움. 모델 업그레이드 시 새 정수 ADR (Amends: ADR-003).

---

## ADR-004 — Strictness 모드 도입

**Date**: 2026-05-25 · **Status**: accepted

**Decision**: 3 모드 — `strict` / `balanced` / `autonomous`. `.harness/config.toml` 선택. 디폴트 `strict`. 하니스 자체 변경은 모든 모드에서 사용자 승인.

**Consequences**: 각 phase Exit 기준에 "어느 모드 사용자 승인" 명시. 모드 전환 자체가 ADR 대상.

---

## ADR-005 — 프로젝트 타입 우선순위

**Date**: 2026-05-25 · **Status**: accepted

**Decision**: `project-types/web-service/` 우선 (깊이). 나머지는 `_generic/` 골격만. 필요 시 dogfood로 확장.

**Consequences**: 첫 실사용 = 웹 프로젝트. 펌웨어/AI 모델은 그 시점 별도 ADR + Phase C 확장.

---

## ADR-006 — Phase A sub-phase별 Codex 리뷰 시점

**Date**: 2026-05-25 · **Status**: accepted (auto-expired after Phase A)

**Decision**: Phase A codex 리뷰는 A.0a / A.0f / A.5 3 시점만 (sub-phase별 별도 리뷰 면제, cost guardrail §5.4 따름).

**자연 종료**: A.4 완료 + §9 폐기 → ADR-006 자동 종료. 이후 모듈마다 phase 04 cross-review 표준.

---

## ADR-007 — §9 Bootstrap exception 폐기 (Phase A.4 완료)

**Date**: 2026-05-25 · **Status**: accepted · **Amends**: HARNESS.md §9

**Decision**: HARNESS §9 임시 게이트 **deprecated**. 모든 phase 진행은 `phases/<phase>.md` 정식 Exit 기준.

**Approval**: user @ 2026-05-25T13:44

---

## 후속

v1.0 이후 (ADR-008~012)는 새 base 결정으로 DECISIONS.md *active section*에 유지.
ADR-001~007 본문은 *current base에 흡수*되어 reference value만 — 본 archive로 분리 (codex meta-review v1.6 M5 cleanup).
